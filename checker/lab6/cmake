set (SOURCES winScreen.c vsTester.c bhTester.c fsTester.c simonMain.c buttonHandler.c flashSequence.c verifySequence.c simonDisplay.c simonControl.c flashSequence.c globals.c)
set (MY_LIBS buttons_switches intervalTimer)

ADD_DEFINITIONS(-DRUN_PROGRAM=4)

add_executable(lab6.elf ${SOURCES})
target_link_libraries(lab6.elf ${330_LIBS} ${MY_LIBS})
set_target_properties(lab6.elf PROPERTIES LINKER_LANGUAGE CXX)

