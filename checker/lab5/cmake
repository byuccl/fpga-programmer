set (SOURCES main.c ticTacToeControl.c ticTacToeDisplay.c minimax.c)
set (MY_LIBS buttons_switches)

ADD_DEFINITIONS(-DMILESTONE=3)

add_executable(lab5.elf ${SOURCES})
target_link_libraries(lab5.elf ${330_LIBS} ${MY_LIBS})
set_target_properties(lab5.elf PROPERTIES LINKER_LANGUAGE CXX)