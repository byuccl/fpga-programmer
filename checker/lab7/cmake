set (SOURCES wamMain.c wamControl.c wamDisplay.c)
set (MY_LIBS buttons_switches)

add_executable(lab7.elf ${SOURCES})
target_link_libraries(lab7.elf ${330_LIBS} ${MY_LIBS})
set_target_properties(lab7.elf PROPERTIES LINKER_LANGUAGE CXX)