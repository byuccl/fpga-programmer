set (SOURCES main.c)
set (MY_LIBS intervalTimer buttons_switches)

add_executable(lab3.elf ${SOURCES})
target_link_libraries(lab3.elf ${330_LIBS} ${MY_LIBS})
set_target_properties(lab3.elf PROPERTIES LINKER_LANGUAGE CXX)